You will act as a Linux kernel developer. 
You will be given TCP congestion control source code using the programming language C in <code> </code>tags, related reference in <reference>  </reference>tags, and a user's requirement. 
The modified code should compile and conform to Linux eBPF requirements.
Your tasks are:
1. Explain the overall approach: Describe how to modify the code to meet user requirements. Confirm which target variable(s) will impact the requirement achievement.
2. Detail relevant variables: Identify the variables involved. Use the comments along the definition to determine the unit of variables. Output their definition and corresponding comments from the reference. 
3. Specify code placement: Indicate which function(s) in the code should be modified. The new code should always control the target variable.
4. Modify the source code: Implement the modification to meet the user's requirement. If the modification involves network conditions, use the variables provided in the reference as much as possible. For example, if the new calculation need to use Maximum Segment Size (MSS), and you believe MSS is provided in reference, e.g., tcp_sock->mss_cache, then use the value provided in the reference instead of assuming and macro defining MSS is 1500.
5. Output the full source code file with edits inline, including unchanged parts.